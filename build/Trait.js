export default class Trait {
    constructor(name) {
        this.NAME = name;
    }

    collides(us, them) {
        
    }

    obstruct(entity, side) {

    }    

    update(entity, deltaTime, room) {

    }
}